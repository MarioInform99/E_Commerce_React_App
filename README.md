# Comercio electronico con React

Para iniciar el proyecto de react app debemos de ejecutar la siguiente sentencia.

```bash
    npx create-react-app <nombre_archivo o ./(si es en el mismo archivo)>
```
